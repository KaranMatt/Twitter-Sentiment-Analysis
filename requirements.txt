tensorflow>=2.13.0   # LSTM and GRU deep learning models, TextVectorization layer
scikit-learn>=1.3.0  # RandomForest, DecisionTree, LogisticRegression, TF-IDF, metrics
nltk>=3.8.1          # TweetTokenizer, stopword removal, WordNet lemmatization
pandas>=2.0.0        # Data loading and manipulation
numpy>=1.24.0        # Numerical computing and array operations
mlflow>=2.5.0        # Experiment tracking for both traditional and deep learning runs
dvc>=3.0.0           # Version control for data (data.dvc) and models (models.dvc)
joblib>=1.3.0        # Saving/loading .pkl artifacts (rf.pkl, tfidf.pkl, TextVectorVocab.pkl)
fastapi>=0.110.0     # REST API framework (/predict/trad-ml and /predict/dl endpoints)
uvicorn>=0.29.0      # ASGI server to run FastAPI
pydantic>=2.0.0      # Request/response schema validation
